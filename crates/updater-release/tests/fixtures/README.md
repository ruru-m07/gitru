# Test-only updater signatures

These files use a disposable Minisign key generated only for the
`updater-release` test suite. The private key is intentionally not stored in
the repository and must never be used for a Gitru release.
