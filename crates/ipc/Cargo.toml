[package]
name = "ipc"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = "1.0.225"
tauri = { version = "2.4.0", features = [] }
uuid = { version = "1", features = ["v4"] }
logger = { path = "../logger" }
log = "0.4"
