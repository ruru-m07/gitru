---
title: Merge Interface
description: Merge flows share conflict UI with rebase mode
icon: GitMerge
---

# Merge Interface

Merge-in-progress detection and a dedicated merge mode will reuse the same operation shell as rebase (continue / abort + conflict resolution).

Today, conflicted files appear in the Changes list once porcelain unmerged entries are parsed. Full merge mode is planned next to rebase.
